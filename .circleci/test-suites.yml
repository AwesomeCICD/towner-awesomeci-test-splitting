name: react-unit-tests
discover: |
  find test/ -type f -name '*_test.ts'
run: |
  vitest run --reporter=junit --outputFile="<< outputs.junit >>" --bail 0 << test.atoms >>
outputs:
  junit: react/junit/junit.xml
options:
  dynamic-test-splitting: true
  test-impact-analysis: false